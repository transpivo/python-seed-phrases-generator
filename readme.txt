made for an hour script that generates sid phrases, may be useful to someone, good luck finding accounts with crypto =)
To use it, open it in an editor and run python main.py After generation, the text document will contain all the phrases, separated by lines
